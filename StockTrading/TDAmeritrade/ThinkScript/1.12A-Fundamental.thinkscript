# Allows you to bring in other securities to the current script or get different aggregation period of the same symbol. 
# https://tlc.thinkorswim.com/center/reference/thinkScript/Functions/Others/Fundamental

# This stores the data by aggregation period
Plot simpleFundamentalData =  Fundamental(FundamentalType.CLOSE, symbol = GetSymbol(), period = AggregationPeriod.MIN, priceType = PriceType.LAST);